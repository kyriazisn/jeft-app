[project]
name = "jeft-app"
version = "0.1.0"
description = "Privacy-first physical gifting platform"
requires-python = ">=3.12"
dependencies = [
  "Django>=5.2,<6.0",
  "psycopg[binary]>=3.2,<4.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0,<9.0",
  "pytest-django>=4.9,<5.0",
  "ruff>=0.8,<1.0",
]
